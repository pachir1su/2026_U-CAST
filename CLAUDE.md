# CLAUDE.md

Claude Code는 작업을 시작하기 전에 저장소 루트의 `AGENTS.md`를 **전부 읽고**
그 규칙을 따릅니다. 저장소 공통 규칙(환경, 원자 커밋, 개인정보 취급, 테스트 표현,
릴리스 정책)은 모두 `AGENTS.md`에 있으며, 이 파일에는 복사하지 않습니다.

Claude Code에만 추가로 적용하는 규칙:

- 관련 GitHub issue의 본문과 댓글을 먼저 읽습니다.
- 구현 전에 변경 파일 계획을 확정하고, 그 목록대로 원자 커밋을 만듭니다.
- 허용된 표적 검증만 실행합니다.
- 완료 전에 적대적 자체 감사를 수행합니다. 자신의 diff를 다시 읽고, 요구가
  실제로 충족됐는지·회귀가 없는지 스스로 반증합니다.
- Draft PR까지만 만들고 자동 병합하지 않습니다.
