# AGENTS.md

이 파일은 이 저장소에서 일하는 **모든 AI 코딩 도구의 공통·표준 지침**입니다.
Claude Code, ChatGPT, Codex 등 어떤 도구로 작업하든 여기 규칙을 먼저 따릅니다.

도구별 진입점(`CLAUDE.md`, `CHATGPT.md`)은 이 문서를 가리키는 짧은 안내일 뿐이며,
같은 내용을 복사해 두지 않습니다. 규칙이 바뀌면 이 파일만 고칩니다.

현재 진행 중 이슈·다음 릴리스 순서처럼 금방 낡는 정보는 여기에 적지 않습니다.
그런 값은 열린 GitHub 이슈와 최근 PR에서 읽습니다.

## 프로젝트

- 천안시 청소년 도시재생 챌린지(U-CAST) 5팀 「건영아 잘하자」의 하드웨어형 산출물입니다.
- 프로젝트 이름은 "멈춰 !"이며, 프로젝트 설명은 "돌발 보행자 감지 및 경고 시스템"입니다.
- 아두이노 우노용 스케치(`main.ino`) 하나와 문서(`README.md`, `docs/`)로 구성된 저장소입니다.
- 핵심 하드웨어 단위는 **시작 IR 1개 + 끝 IR 1개 + LED 스트립 1줄 = 독립 채널 1개**입니다. 채널 간 시작·종료 상태를 합쳐 한 전역 횡단으로 처리하지 않습니다(이슈 #18).
- 문서와 사용자 안내는 한국어를 기본으로 작성합니다.

## 환경

- 개발은 Windows, 실행은 Arduino IDE + 아두이노 우노 실물 보드입니다.
- 빌드·업로드는 Arduino IDE에서 수행하며, 저장소에 빌드 산출물을 커밋하지 않습니다.
- 외부 라이브러리는 `Adafruit NeoPixel` 하나만 사용합니다. 새 라이브러리를 임의로
  추가하지 않습니다.
- 핀 번호·픽셀 수·임계값 같은 조정값은 반드시 `main.ino` 상단 `[설정]` 구역의 상수로
  둡니다. 로직 중간에 숫자를 박아 넣지 않습니다.
- 실물 보드에 연결하지 않은 상태에서 "실기기에서 확인했다"고 말하지 않습니다.

## 작업 시작 전에 읽을 것

작업을 시작하기 전에 최소한 다음을 실제로 읽습니다.

```text
README.md
main.ino
docs/ 하위 관련 문서
관련 issue 본문 + 댓글
최근 관련 PR
```

- 업로드된 ZIP 요약이나 이전 PR의 "했다"는 보고만 믿지 않습니다. 실제 파일과
  diff를 직접 확인합니다.
- 이슈를 확인할 때는 본문뿐 아니라 댓글도 모두 읽고 근거로 삼습니다.
- 닫힌 이슈는 사용자가 명시적으로 요청하지 않는 한 읽거나 근거로 삼지 않습니다.

## 사용자 요구 해석

- 사용자가 "예를 들어"라고 말한 항목은 **예시**입니다. 그 항목만 고치고 끝내지
  않습니다.
- 같은 패턴의 인접 문서·함수·오류 경로를 검색해 함께 고칩니다.
- 다만 범위를 무제한으로 넓히지 않습니다. 요청한 결함과 같은 원인·같은 패턴까지가
  경계입니다.
- 불확실한 요구사항은 구현 전에 질문합니다. 질문은 실제 비밀값이 필요하거나,
  데이터 삭제·비가역 작업이 있거나, 서로 배타적인 제품 요구가 있거나, 사용자
  선택에 따라 결과가 크게 달라지는데 저장소 근거로 판단할 수 없을 때만 합니다.
- 요구와 안전한 구현 방식이 충분히 명확하고 되돌릴 수 있으면 먼저 조치한 뒤
  완료 보고에서 변경 내용과 이유를 설명합니다.

## 구현 계획 품질

구현 전에 확정하는 계획에는 최소한 다음이 들어갑니다.

```text
정확한 파일
함수 / 상수
현재 문제
수정할 control flow
핀 배정과 배선 영향
오류 분기
테스트 계약
실기기 QA 항목
```

자연어 요구만 반복해 옮겨 적는 것은 계획이 아닙니다.

## 코드 변경

- 기존 구조와 네이밍 스타일을 우선 따릅니다(`PIN_*`, `*_MS`, `HoldFilter` 등).
- 불필요한 리팩터링과 범위 밖 변경은 피합니다.
- 같은 정책을 두 곳에 복사하지 않습니다. 상태 전이·센서 확정 필터처럼 동작 기준이
  걸린 로직은 기존 함수를 재사용합니다.
- `delay()`를 새로 쓰지 않습니다. 시간 처리는 `millis()` 기반으로 유지합니다.
- 핀 배정을 바꾸면 `README.md` 핀 연결표와 `docs/` 문서, 배선도 이미지도 같은 PR에서
  함께 갱신합니다.

## 테스트와 표현

실행한 검증을 실제 범위 그대로 구분해 보고합니다.

```text
코드 정독 / 정적 검토
Arduino IDE 컴파일 검증
Tinkercad 등 시뮬레이터
브레드보드 실물 회로
발표 시연 리허설
```

- 컴파일 통과를 실물 동작 확인이라고 주장하지 않습니다.
- 실행하지 않은 검사는 "실행하지 않음"으로 명시합니다.
- 코드 변경이 있으면 가능한 범위에서 표적 검증을 실행합니다.
- 문서만 변경한 경우에도 링크와 명령 예시, 핀 번호 일치 여부를 직접 확인합니다.

## 개인정보와 배포 자료

- 비밀값, 실제 토큰, 비밀번호, 개인 설정 파일을 출력하거나 커밋하지 않습니다.
- 팀원·멘토의 실명 전체, 연락처, 학교/학년 등 개인정보를 문서에 새로 적지 않습니다.
  기존 문서의 표기 수준(예: `안OO`)을 그대로 따릅니다.
- **멘토 안내 자료 등 외부에 공개하면 안 되는 배포 자료는 저장소에 올리지 않습니다.**

## Git 원자성

- **모든 PR에서 변경 파일 수와 커밋 수는 정확히 같아야 합니다(조건 없이 항상 적용되는 저장소 기본 규칙).**
  - 커밋 하나는 정확히 파일 하나만 변경합니다.
  - 변경 파일 하나는 정확히 커밋 하나에만 등장합니다.
  - 같은 파일을 여러 커밋에서 나눠 수정하지 않습니다.
  - 추가, 수정, 삭제, rename 파일을 모두 변경 파일 수에 포함합니다.
  - PR 생성 전에 rebase/reset/recommit 으로 기록을 정리합니다.
  - merge commit 은 작업 브랜치 커밋 수에 포함되지 않도록 유지합니다.
  - 생성 산출물도 변경 파일이면 별도 커밋 하나를 사용합니다.
  - `AGENTS.md`, `CLAUDE.md`, `CHATGPT.md` 도 각각 별도 커밋입니다.
  - 최종 변경 파일 수와 커밋 수가 같은지, 각 커밋이 파일 하나만 변경했는지 스크립트로 검증합니다.

## 이슈와 PR

- **PR 댓글의 "ㅇㅇ"는 "곧 머지하겠다"는 뜻입니다.** 추가 작업을 시작하라는
  지시가 아니므로, 그걸 근거로 새 작업을 벌이지 않습니다. 다음 작업은
  사용자가 명시적으로 요청할 때만 시작합니다.
- PR은 카테고리 단위로 나누고, 커밋은 이슈나 작업 단위로 작게 쪼갭니다.
- PR 본문에는 관련 이슈 번호를 명확히 연결합니다.
- QA·검증할 항목이 있으면 PR 본문에 정리해 넣습니다(무엇을 어떻게 확인했는지,
  남은 확인 사항 포함).
- 사용자가 직접 해야 할 일(부품 확인, 실물 배선, 시연 리허설 등)이 있으면
  하나하나 구체적으로 알려줍니다.
- **PR을 주기적으로(예: 1시간마다) 자동 재검증하거나 셀프 체크인을 예약하지
  않습니다.** 사용자가 명시적으로 요청할 때만 합니다.

## 릴리스

- Git 태그를 만들지 않습니다.
- GitHub Release를 발행하지 않습니다.
- 자동 merge 하지 않습니다.
- changelog에 확정 버전처럼 기록하지 않습니다.

### 다음 릴리즈 버전 추천

- 작업을 마칠 때 현재 프로젝트 버전과 변경 범위를 확인하고 다음 릴리즈 버전을 추천합니다.
- 현재 버전은 저장소의 실제 기준 파일, 태그, 릴리즈 기록 등을 확인해 판정합니다.
  기준 파일이 없으면 "버전 기준 없음"이라고 보고하고 임의로 만들지 않습니다.
- 여러 곳의 버전이 서로 다르면 임의로 결정하지 말고 불일치 내용을 보고합니다.
- Semantic Versioning 기준으로 다음 버전을 판단합니다.

예시:

- 현재 버전이 v0.14.5일 때 버그 수정, 문서 수정, 테스트 보강, 기존 동작을 유지하는 UI 수정만 있다면 v0.14.6을 추천합니다.
- 호환성을 유지하면서 사용자 기능, 화면, 데이터 구조가 추가된다면 v0.15.0을 추천합니다.
- 공개 동작, 핀 배정, 설정 형식, 설치·운영 방식에 의도적인 호환성 파괴가 있거나 프로젝트가 공식 안정 버전 기준을 충족할 때만 v1.0.0 또는 다음 major를 추천합니다.
- 작업량이 많다는 이유만으로 major 버전을 추천하지 않습니다.
- 0.x 버전에서는 호환성 변화 여부도 별도로 명시합니다.

완료 보고에는 반드시 다음을 포함합니다.

- 현재 버전
- 추천하는 다음 버전
- patch/minor/major 중 어떤 판단인지
- 판단 이유
- 호환성 파괴 여부
- 마이그레이션 필요 여부
